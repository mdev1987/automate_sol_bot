# Take Profit Target 2x
# Stop Loss  0.82x (18%)
# Poll Interval 8  (default is 8s)

# Dexscreener as primary price source. Jupiter quote as fallback. Dead pool
# detection if liquidity drop below $25
# on dead pool we handling the dead pool instead of retrying forever.
