# Monitor: bot.log + journal.json + trade_log.csv
# report to telegram bot
# make it auto restart if bot crashed


# Tunnig

# Start with Dry_RUN=true   verify scanner find tokens
# Review scores: are qualifyig tokens reasonable?
# Go live with $2 USDC - Minimal risk while validating
# Watch 5-10 cycles. check win rate, ave returns
# Adjust: slippage, stop loss, take profit, filters
# Scale up STARTING_AMOUNT only after proven profit


# The scanner listens specifically to pump.fun
# launches, the jupiter routes the swap through whichever DEX has
# the best price
